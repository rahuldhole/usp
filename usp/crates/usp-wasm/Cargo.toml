[package]
name = "usp-wasm"
version = "0.1.0"
edition = "2021"
description = "WebAssembly bindings for Unified State Protocol (USP)"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
usp-core = { path = "../usp-core" }
wasm-bindgen = "0.2"
serde_json = "1.0"
js-sys = "0.3"
