[package]
name = "usp-core"
version = "0.1.0"
edition = "2021"
description = "Pure Rust reference protocol engine for Unified State Protocol (USP)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[target.'cfg(target_arch = "wasm32")'.dependencies]
js-sys = "0.3"
